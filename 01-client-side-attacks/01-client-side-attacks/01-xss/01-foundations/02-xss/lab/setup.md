# Lab Setup Guide — XSS Node.js Lab

## 🧠 Goal
Set up a local vulnerable Node.js application and run it safely on your machine for XSS testing.

---

## ⚙️ Requirements

Make sure you have:

- Node.js (v16+ recommended)
- npm (comess with Node.js)

Check versions:

```bash
node -v
npm -v
